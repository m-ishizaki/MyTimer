<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:MyTimer"
             x:Class="MyTimer.CountDownPage"
             NavigationPage.HasNavigationBar="False"
             xmlns:vm="clr-namespace:MyTimer.ViewModels"
             Appearing="CountDownPageAppearing"
             Disappearing="CountDownPageDisappearing"
             Title="カウントダウン">
    <ContentPage.BindingContext>
        <vm:CountDownPageViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <Grid>
            <Image Source="{local:ImageResource MyTimer.Resources.Background.png}" Aspect="AspectFit" />
            <Grid BackgroundColor="#80FFFFFF"/>

            <Label Text="{Binding Time, StringFormat='{0:mm\\:ss\\.fff}'}" HorizontalOptions="Center" VerticalOptions="Center" FontSize="96" TextColor="Black" FontAttributes="Bold"/>
            <Button Text=" &lt; Back " Command="{Binding GoBackCommand}" BackgroundColor="#80808080" HorizontalOptions="Start" VerticalOptions="Start"/>
        </Grid>
    </ContentPage.Content>
</ContentPage>